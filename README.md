# QR-Secret-Messages-Encoder-Decoder
A Python-based project that allows users to hide and retrieve secret messages inside QR codes. The application encodes text into a QR code and provides functionality to decode and extract the hidden message.
